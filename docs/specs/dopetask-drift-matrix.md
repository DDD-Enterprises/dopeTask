# dopeTask Drift Matrix

[CODE] Each row records a concrete mismatch or duplicated authority surface observed during code, docs, schema, test, and runtime-help inspection.
[CODE] Claim sources are the surfaces making or implying the behavior; code-reality sources are the runtime or implementation surfaces actually enforcing it.

| ID | Topic | Claim source | Code reality source | Drift type | Severity | Current impact | Recommended disposition |
| --- | --- | --- | --- | --- | --- | --- | --- |
| D001 | `tp series import` is defined in source and docs but unavailable at runtime | [DOCS/PLANS] `docs/13_TASK_PACKET_FORMAT.md`, `docs/22_WORKFLOW_GUIDE.md`; [CODE] `src/dopetask/ops/tp_series/cli.py` declares `@app.command("import")`. | [CODE] Runtime `python -m dopetask tp series --help` lists only `exec`, `status`, `finalize`; `python -m dopetask tp series import --help` returns `No such command 'import'`. | runtime/source mismatch | high | Operators following the documented import flow cannot invoke the command in the shipped CLI. | Decide whether `import` is intended to be public. Either make it visible/working in the CLI or remove/replace all import guidance. |
| D002 | `execute` is defined in `cli.py` but absent from runtime command registration | [CODE] `src/dopetask/cli.py` defines `@cli.command(name="execute")` as automatic next-runnable packet execution. | [CODE] `python -m dopetask --help` does not list `execute`, and `python -m dopetask execute --help` returns `No such command 'execute'`. | runtime/source mismatch | medium | Source readers can infer an available user-facing command that operators cannot actually invoke. | Either register the command correctly or remove dead/public-facing source and docs references. |
| D003 | Low-level executor help advertises multiple agents, but runtime implementation only supports Gemini | [CODE] Runtime `python -m dopetask tp exec --help` and source `src/dopetask/ops/tp_exec/cli.py` advertise `gemini, codex, or vibe`; `src/dopetask/ops/tp_series/cli.py` repeats the same option help. | [CODE] `src/dopetask/ops/tp_exec/engine.py` instantiates `GeminiAdapter` for `agent == "gemini"` and raises `ValueError` for every other profile. | runtime/source mismatch | high | The user-visible CLI suggests supported agent profiles that will fail at execution time. | Narrow the option help to Gemini only, or implement the missing engines before advertising them. |
| D004 | Series-first docs describe one default path, while the runtime still presents multiple first-class execution families | [DOCS/PLANS] `README.md`, `docs/22_WORKFLOW_GUIDE.md`, and `docs/24_UPGRADE_GUIDE.md` position JSON TP series as the current default. | [CODE] Runtime `python -m dopetask --help` still exposes top-level `route`, `orchestrate`, `compile-tasks`, `run-task`, `collect-evidence`, `gate-allowlist`, `commit-run`, `loop`, `wt`, and `finish` families alongside `tp`. | stale-default | medium | The product story is series-first, but the CLI surface still reads like a multi-era tool with several coequal paths. | Either demote legacy/transitional commands in help/docs or document the supported execution families explicitly instead of implying a single public path. |
| D005 | Docs cite `docs/schemas/task_packet.schema.json` as packet authority, but runtime validation loads packaged `dopetask_schemas` | [DOCS/PLANS] `docs/23_INTEGRATION_GUIDE.md` tells integrators to match `docs/schemas/task_packet.schema.json`. | [CODE] `src/dopetask/utils/schema_registry.py` and `src/dopetask/schemas/validator.py` load schemas exclusively from the installed `dopetask_schemas` package. | duplicate-surface | high | Consumers can pin themselves to a documentation copy that is not the runtime lookup source. | Name `dopetask_schemas/*.schema.json` as the runtime authority and treat `docs/schemas/` as documentation mirrors only if they remain synchronized automatically. |
| D006 | The repo carries three task-packet schema surfaces with different implied authority levels | [DOCS/PLANS] `docs/schemas/task_packet.schema.json` is a documentation copy; [CODE] `dopetask_schemas/task_packet.schema.json` is packaged runtime data; [CODE] `schemas/` hosts other formal schema artifacts used by code and docs. | [CODE] Runtime packet validation goes through `dopetask_schemas`; `schemas/` and `docs/schemas/` still exist as parallel surfaces in the tree. | duplicate-surface | medium | Contract readers must guess which copy is canonical, raising the risk of silent shape drift. | Publish an explicit schema-authority policy and wire docs copies from the packaged source or remove redundant mirrors. |
| D007 | Architecture spec overstates what `TaskExecutor` currently enforces | [DOCS/PLANS] `docs/architecture/dopetask-supervisor-executor-adapter-spec.md` says `TaskExecutor` manages status transitions and orchestrates fail-closed logic. | [CODE] `src/dopetask/pipeline/task_runner/executor.py` only validates adapter return types and raises on `failed` results; status transition management is not implemented there. | conflict | medium | The architecture doc reads as a more mature execution kernel than the current runtime actually provides. | Either tighten the doc to current behavior or finish the executor implementation before treating the spec as normative. |
| D008 | Architecture spec collapses low-level execution and series worktree lifecycle into one kernel story | [DOCS/PLANS] `docs/architecture/dopetask-supervisor-executor-adapter-spec.md` describes worktree creation, validation, commit/promotion, and removal as part of the kernel contract. | [CODE] `src/dopetask/ops/tp_exec/engine.py` performs low-level packet execution and proof aggregation only; worktree creation, allowlist commit, and cleanup live in `src/dopetask/ops/tp_series/logic.py`. | conflict | medium | It is unclear which layer owns execution semantics versus git lifecycle semantics. | Separate the low-level executor contract from the series wrapper contract in docs and naming. |
| D009 | Integration-guide proof example is not schema-complete for the formal proof bundle contract | [DOCS/PLANS] `docs/23_INTEGRATION_GUIDE.md` shows a shortened proof JSON example intended for external consumption. | [CODE] `proof/standards/PROOF_BUNDLE_SCHEMA.json` requires `summary.key_caveats`, `acceptance_checks`, `validation`, `artifacts.primary`, `artifacts.supporting`, archive `sha256`, and `manifest`; `tests/unit/obs/test_proof_contract_assets.py` validates bundles against that schema. | conflict | high | Integrators may treat an illustrative example as canonical and build consumers that reject real bundles or miss required fields. | Mark the example as partial/non-canonical or replace it with a schema-complete example that matches the tested standard. |
| D010 | Router docs and router code remain markdown-hint-centric inside a repo that otherwise markets JSON packets as the default | [DOCS/PLANS] `docs/12_ROUTER.md` documents `PACKET.md` routing and `ROUTER_HINTS`; `docs/22_WORKFLOW_GUIDE.md` and `docs/24_UPGRADE_GUIDE.md` market JSON TP series as the default execution path. | [CODE] `src/dopetask/router/planner.py` still reads shallow text `ROUTER_HINTS` from the packet file via `extract_router_hints`, while JSON packet handling is centered elsewhere. | partial | medium | Routing remains a live subsystem, but its input model and docs belong to an older packet era. | Either keep router explicitly legacy/manual or define a JSON-native router hint contract. |
| D011 | `docs/11_PUBLIC_CONTRACT.md` still frames route artifacts as the principal output surface | [DOCS/PLANS] `docs/11_PUBLIC_CONTRACT.md` lists route-plan artifacts under `out/dopetask_route/` as the output contract. | [CODE] `README.md`, `docs/22_WORKFLOW_GUIDE.md`, `docs/24_UPGRADE_GUIDE.md`, `src/dopetask/ops/tp_series/logic.py`, and runtime help position proof bundles plus `out/tp_series/...` as the default operator ledger for current workflows. | stale-default | medium | The documented public contract is narrower and older than the execution surfaces operators are told to use today. | Refresh the public-contract doc around current default outputs, while explicitly listing transitional route/orchestrate artifacts separately. |
| D012 | Multi-mode execution plan references future shell-adapter work that is not present in the tree | [DOCS/PLANS] `docs/plans/dopetask-multi-mode-execution-plan.md` Phase 3 references `src/dopetask_adapters/shell/` and mixed shell/agent support. | [CODE] No `src/dopetask_adapters/shell/` package exists in the tracked source tree; the current low-level executor remains Gemini-only. | partial | low | The plan is useful historical intent, but it should not be read as current capability. | Label the plan as historical/incomplete or update it with actual implementation status. |
| D013 | The current Gemini runtime path is command-driven shell execution, not a live provider session | [DOCS/PLANS] `docs/architecture/dopetask-supervisor-executor-adapter-spec.md` and `docs/23_INTEGRATION_GUIDE.md` describe provider-specific adapters as execution shims for LLM providers. | [CODE] `src/dopetask_adapters/gemini/executor.py` compiles prompt text but does not send it anywhere; `src/dopetask_adapters/gemini/step_runner.py` executes `commands` and `validation` locally via `subprocess.run(shell=True)`. | partial | medium | Operator expectations about what the `gemini` profile means can outrun the actual shell-based runtime behavior. | Document the current `gemini` profile honestly as a transitional/local executor, or finish the provider integration before using model-centric wording as product truth. |

